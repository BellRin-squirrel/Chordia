name: Build Dev Client (iOS & Android Debug)
on:
  workflow_dispatch: # 手動実行ボタンを有効化

defaults:
  run:
    working-directory: ./mobile

jobs:
  # ==========================================
  # iOS Dev Clientビルド (Debug)
  # ==========================================
  build_ios_dev:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm install

      - name: Give permission to prebuild
        run: chmod +x ./node_modules/.bin/expo

      # ネイティブコードの生成
      - name: Prebuild (Convert to Native Project)
        run: ./node_modules/.bin/expo prebuild --platform ios --clean --non-interactive

      - name: Give execute permission to all shell scripts in node_modules
        run: |
          find node_modules -type f -name "*.sh" -exec chmod +x {} \;

      - name: Configure Node for Xcode Build
        run: |
          NODE_PATH=$(which node)
          echo "export NODE_BINARY=$NODE_PATH" > ios/.xcode.env.local
          cat ios/.xcode.env.local

      - name: Get Workspace Name
        run: |
          PROJECT_NAME=$(ls ios | grep .xcworkspace | sed 's/\.xcworkspace//')
          echo "PROJECT_NAME=$PROJECT_NAME" >> $GITHUB_ENV

      # ★変更点: -configuration Debug を指定してDev Clientを作成
      - name: Make archive for iOS Dev Client (Unsigned)
        run: |
          set -o pipefail
          xcodebuild -workspace "./ios/$PROJECT_NAME.xcworkspace" \
            -scheme "$PROJECT_NAME" \
            -configuration Debug \
            -sdk iphoneos \
            -archivePath "./build/$PROJECT_NAME.xcarchive" \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            ENABLE_USER_SCRIPT_SANDBOXING=NO 2>&1 | xcpretty

      - name: Make Payload Folder and Make It IPA file
        run: |
          mkdir Payload
          cp -r "./build/$PROJECT_NAME.xcarchive/Products/Applications/$PROJECT_NAME.app" ./Payload/
          zip -r Chordia-DevClient.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Chordia-DevClient-iOS-IPA
          path: mobile/Chordia-DevClient.ipa
          
      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ios-dev-build-log
          path: mobile/build.log

  # ==========================================
  # Android Dev Clientビルド (Debug)
  # ==========================================
  build_android_dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm install

      - name: Give permission to prebuild
        run: chmod +x ./node_modules/.bin/expo

      # ★既存のパッチもそのまま適用
      - name: Patch react-native-track-player for Kotlin 2.x
        run: |
          node -e '
          const fs = require("fs");
          const file = "node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt";
          if (fs.existsSync(file)) {
            let content = fs.readFileSync(file, "utf8");
            content = content.split("Arguments.fromBundle(").join("fromBundleSafe(");
            const helper = `\n
          private fun fromBundleSafe(bundle: android.os.Bundle?): com.facebook.react.bridge.WritableMap {
              return if (bundle != null) com.facebook.react.bridge.Arguments.fromBundle(bundle) else com.facebook.react.bridge.Arguments.createMap()
          }
          `;
            content += helper;
            fs.writeFileSync(file, content);
          }
          '

      - name: Prebuild (Convert to Native Project)
        run: ./node_modules/.bin/expo prebuild --platform android --clean --non-interactive

      # ★変更点: assembleRelease ではなく assembleDebug でDev Clientを作成
      - name: Build Android APK (Debug)
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Chordia-DevClient-Android-APK
          path: mobile/android/app/build/outputs/apk/debug/app-debug.apk